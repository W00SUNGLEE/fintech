//function plus(p1, p2) {
//    return p1 + p2
//}


const plus = (p1, p2) => { 
    return p1 + p2 
}


console.log(plus(2, 5))
console.log(plus(2, 9))

//#work 1 빼기, 나누기, 곱하기 기능 선언 하기
const sub = (x, y) => {
    return x - y
}

const div = (x, y) => {
    return x / y
}

const mul = (x, y) => {
    return x * y
}

console.log(sub(4, 2))
console.log(div(4, 2))
console.log(mul(4, 2))